# 🏗️ 구현 세부사항

## 📋 완성된 컴포넌트

### 1. ContractSummaryTable (계약현황 요약)
**위치**: `src/components/tables/ContractSummaryTable.jsx`

**기능**:
- ✅ 총 계약수, 월 보험료 요약 카드
- ✅ 보험사별 계약 통계 테이블
- ✅ 보험료 비중 시각화 (진행률 바)

**데이터 구조**:
```javascript
{
  customerInfo: {
    name: string,
    age: number,
    gender: string,
    monthlyPremium: number
  },
  contracts: [{
    company: string,
    premium: number
  }]
}
```

---

### 2. ContractListTable (전체 계약 리스트)
**위치**: `src/components/tables/ContractListTable.jsx`

**기능**:
- ✅ 상품별 상세 정보 (보험사, 상품명, 가입일)
- ✅ 납입방식, 납입기간, 만기 표시
- ✅ 월 보험료 및 합계 계산
- ✅ 안내사항 가이드

**테이블 컬럼**:
| No. | 보험사 | 상품명 | 가입일 | 납입방식 | 납입기간 | 만기 | 월 보험료 |
|-----|--------|--------|--------|----------|----------|------|-----------|

---

### 3. CoverageStatusTable (담보별 현황)
**위치**: `src/components/tables/CoverageStatusTable.jsx`

**기능**:
- ✅ 카테고리별 그룹화 (사망·장해, 암 진단, 뇌/심장, 실손)
- ✅ 현재 보장 vs 권장 보장 비교
- ✅ 충족률 계산 및 시각화
- ✅ 색상 코딩 (부족: 빨강, 보통: 노랑, 충분: 초록)

**충족률 기준**:
- 🔴 부족: 70% 미만
- 🟡 보통: 70-99%
- 🟢 충분: 100% 이상

---

### 4. DiagnosisTable (담보별 진단현황)
**위치**: `src/components/tables/DiagnosisTable.jsx`

**기능**:
- ✅ 상태별 통계 요약 (부족/미가입/충분)
- ✅ 담보별 진단 결과 테이블
- ✅ 차액 계산 및 표시
- ✅ 상태 뱃지 (아이콘 + 색상)

**진단 상태**:
- ⚠️ 부족: 권장 금액에 미달
- ⊗ 미가입: 보장 없음
- ✓ 충분: 권장 금액 충족

---

## 🎨 디자인 시스템

### 색상 팔레트 (Teal)
```javascript
primary: {
  50: '#f0fdfa',   // 배경
  100: '#ccfbf1',  // 밝은 배경
  200: '#99f6e4',  // 테두리
  300: '#5eead4',
  400: '#2dd4bf',
  500: '#14b8a6',  // 🎯 메인 컬러
  600: '#0d9488',  // 헤더, 버튼
  700: '#0f766e',  // 강조 텍스트
  800: '#115e59',
  900: '#134e4a',
}
```

### 타이포그래피
- **폰트**: Noto Sans KR (Google Fonts)
- **제목**: 700 (Bold), 18-24px
- **본문**: 400 (Regular), 14px
- **캡션**: 400, 12px

### 간격 체계
- 섹션 간격: 32px (`space-y-8`)
- 카드 패딩: 24px (`p-6`)
- 테이블 셀: 12px 16px (`px-4 py-3`)

---

## 🔧 PDF 파싱 로직

### 파일: `src/utils/pdfParser.js`

#### 주요 함수:

**1. parsePDF(file)**
- PDF 파일을 ArrayBuffer로 변환
- PDF.js로 모든 페이지 텍스트 추출
- `parseInsuranceData()` 호출

**2. parseInsuranceData(text)**
- 고객 정보 추출
- 계약 리스트 추출
- 담보 현황 추출
- 진단 현황 추출

**3. extractCustomerInfo(text)**
```javascript
// 패턴: "안영균 (61세 ,남자)"
const nameMatch = text.match(/([가-힣]+)\s*\((\d+)세\s*,\s*(남자|여자)\)/);
```

**4. extractContracts(text)**
```javascript
// 패턴: 번호, 회사명, 상품명, 가입일, 납입방식, 기간, 만기, 보험료
const contractPattern = /(\d+)\s+([\w가-힣]+)\s+...$/g;
```

**5. parseAmount(amountStr)**
```javascript
// "1억 3,000만" → 13000 (만원 단위)
// "5,000만" → 5000
```

---

## 📦 상태 관리 (Zustand)

### 파일: `src/store/insuranceStore.js`

```javascript
{
  parsedData: null | object,  // 파싱된 데이터
  isLoading: boolean,         // 로딩 상태
  error: string | null,       // 에러 메시지
  
  // 액션
  setParsedData(data),
  setLoading(boolean),
  setError(string),
  reset()
}
```

---

## 🖨️ 인쇄 최적화

### CSS 미디어 쿼리
```css
@media print {
  @page {
    size: A4;
    margin: 0;
  }
  
  .no-print {
    display: none !important;
  }
  
  .page-break {
    page-break-after: always;
  }
}
```

### 인쇄 시 숨김 요소
- 헤더 네비게이션
- 파일 업로드 영역
- 버튼 (인쇄하기, 다시 업로드)
- 푸터

---

## 🚀 성능 최적화

### 1. 청크 분리
- Vite 자동 코드 스플리팅
- PDF.js는 별도 청크 (522KB)

### 2. 느긋한 로딩
- PDF 파싱은 백그라운드에서 수행
- 로딩 인디케이터 표시

### 3. 메모이제이션
- React 컴포넌트 최적화 가능 (필요시 `useMemo` 추가)

---

## 🧪 테스트 데이터

### 샘플 PDF 구조
```
표지
└─ 이름: 안영균 (61세, 남자)

전체 보장현황
├─ 상해사망: 1억 6,000만 / 권장 1억
├─ 일반암: 1억 3,000만 / 권장 1억
└─ ...

계약 리스트
├─ 1. 메리츠화재 - 암보험 - 60,590원
├─ 2. 메리츠화재 - 알파Plus - 144,630원
└─ ...
```

---

## 🔮 확장 가능성

### 추가 가능한 기능
1. **차트 라이브러리 통합** (Recharts, Chart.js)
2. **엑셀 내보내기** (xlsx 라이브러리)
3. **PDF 직접 생성** (jsPDF, pdfmake)
4. **다크 모드** (Tailwind 다크 모드)
5. **다국어 지원** (i18next)

### API 연동 (향후)
```javascript
// Gemini API 예시
const analyzeInsurance = async (data) => {
  const response = await fetch('https://generativelanguage.googleapis.com/v1beta/models/gemini-pro:generateContent', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${API_KEY}`
    },
    body: JSON.stringify({
      contents: [{
        parts: [{
          text: `보험 분석: ${JSON.stringify(data)}`
        }]
      }]
    })
  });
  return response.json();
};
```

---

## 📊 데이터 흐름

```
PDF 파일
  ↓
pdfParser.js (텍스트 추출)
  ↓
insuranceStore (상태 저장)
  ↓
ReportViewer
  ├─ ContractSummaryTable
  ├─ ContractListTable
  ├─ CoverageStatusTable
  └─ DiagnosisTable
```

---

## ✅ 체크리스트

- [x] 프로젝트 초기화
- [x] TailwindCSS 청녹색 테마 설정
- [x] PDF 파싱 로직 구현
- [x] Zustand 상태 관리
- [x] 4가지 테이블 컴포넌트
- [x] 파일 업로드 UI
- [x] 인쇄 최적화
- [x] 반응형 디자인
- [x] 빌드 및 배포 준비
- [ ] 생애설계 그래프 (대기)
- [ ] AI 분석 보고서 (대기)

---

**완성도: 80% (핵심 기능 완료)**

다음 단계는 사용자의 요구사항에 따라 진행됩니다! 🎉
