{
  "name": "cloudflare-poc",
  "version": "1.0.0",
  "description": "Cloudflare Workers POC for Insurance Report AI Validator (5min CPU time)",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "keywords": ["cloudflare", "workers", "ai", "insurance", "pdf"],
  "author": "InsuReport Team",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^4.50.0"
  }
}
