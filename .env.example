# ======================
# Gemini AI 설정
# ======================

# Gemini API Key (Vercel Serverless Function에서만 사용)
# https://aistudio.google.com/app/apikey 에서 발급
GEMINI_API_KEY=your-gemini-api-key-here

# AI 검증 활성화 여부 (프론트엔드)
VITE_USE_AI_VALIDATION=true

# API Base URL (프론트엔드)
# 로컬 개발: http://localhost:5173
# Vercel 배포: 빈 문자열 (동일 도메인 사용)
VITE_API_URL=

# ======================
# Cloudflare R2 설정 (대용량 PDF 처리용)
# ======================

# Cloudflare 계정 ID
# Cloudflare 대시보드 → R2 → 우측 상단에서 확인
R2_ACCOUNT_ID=your-account-id-here

# R2 API 토큰 (Access Key ID)
# Cloudflare 대시보드 → R2 → Manage R2 API Tokens → Create API Token
R2_ACCESS_KEY_ID=your-access-key-id-here

# R2 API 토큰 (Secret Access Key)
R2_SECRET_ACCESS_KEY=your-secret-access-key-here

# R2 버킷 이름
# Cloudflare 대시보드 → R2 → Create Bucket
R2_BUCKET_NAME=insu-report-pdfs
