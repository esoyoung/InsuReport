# Production Environment Variables (Build-time)
# These are injected into the Vite build at compile time

# Enable AI validation with Anthropic Claude and OpenAI GPT-4o
VITE_USE_AI_VALIDATION=true

# API URL (defaults to same domain for Cloudflare Pages)
# VITE_API_URL=
